Section4/16:

<dependencies>
  	<!-- https://mvnrepository.com/artifact/io.rest-assured/rest-assured -->
	<dependency>
	    <groupId>io.rest-assured</groupId>
	    <artifactId>rest-assured</artifactId>
	    <version>5.5.6</version>
	</dependency>
		<!-- https://mvnrepository.com/artifact/org.testng/testng -->
	<dependency>
	    <groupId>org.testng</groupId>
	    <artifactId>testng</artifactId>
	    <version>7.11.0</version>
	</dependency>
		<!-- https://mvnrepository.com/artifact/org.hamcrest/hamcrest -->
	<dependency>
	    <groupId>org.hamcrest</groupId>
	    <artifactId>hamcrest</artifactId>
	    <version>3.0</version>
	</dependency>
  </dependencies>

  Section4/17:

  //Validate if Add Place is working as expected
		
		// Rest Assured work on 3 principals
		// Given, When, Then
		
		//Given- Input All Details which is required by API
		//When- Submit the API/Hit this API Resource and HTTP method will go under "When"
		//Then- Validate API/response


Problem 1: RestAssured library was not getting imported
Solution: pom.xml -> Run As -> Maven Clean 

Problem 2: While adding Body for API in eclispe if escape character are not automatically added
Solution: Preferences (Eclipse -> Settings -> Java -> Editor -> Typing -> Check Escape text when pasting into a string literal)

Section5/20:

JsonPath : It is a class which takes input as string and convert that into JSON
JsonPath -> io.restassured.path.json





